using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Corridor_Boi : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        //Run Check timer
    }

    // Update is called once per frame
    void Update()
    {
        //If check timer = 0, run Check
    }

    void CheckTimer ()
        {
            //Start timer (300 to 0)
            //If Noise loud = true, -100 seconds to timer
            //If timer = 0, restart timer
        }

    void Check ()
    {
        //Start FeFiFo timer (3 -> 0)
        //Check Player Pretend status
        //If player pretendstatus = not true
        //initiate death
        //If pretendStatus = true, Reset Check timer
    }
}
